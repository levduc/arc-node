# lean-native — v0.1 snapshot (historical)

This is the diverged v0.1 copy the 2026-08 campaign ran. The live lean node,
wire format and load generator are the `lean-lane` repository (branch
`v0.3-dev`); nothing current builds from this crate.
